# Copy to .env for local development. Never commit real secrets.
# On Railway, set these via the project's Variables tab (and link a Postgres plugin
# which provides DATABASE_URL automatically).

PORT=3000
DATABASE_URL=postgres://myuser:password@localhost:5432/topminton
SESSION_SECRET=change-to-a-secure-string
NODE_ENV=development
